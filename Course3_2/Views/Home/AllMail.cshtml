
@{
    ViewBag.Title = "AllMail";
}

<h2>AllMail</h2>

@model List<MicroServiceCore.Model.EmailMessage>
@foreach (var element in Model)
{
    <p>@Html.DisplayFor(m => element.FromAddress)</p>
    <p>@Html.DisplayFor(m => element.Subject)</p>
    <p>@Html.DisplayFor(m => element.Message)</p>
}

